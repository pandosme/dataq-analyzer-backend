# Developer build file — used to build and push the image to Docker Hub.
# End-users should use docker-compose.yml (no build required).
#
# Usage:
#   docker compose -f docker-compose.build.yml build
#   docker compose -f docker-compose.build.yml push

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: pandosme/dataq-backend:latest
